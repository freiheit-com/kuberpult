name: "Execution Plan on main"
'on':
  push:
    branches:
      - main
jobs:
  execution-plan-pr:
    uses: ./.github/workflows/execution-plan-snippet.yml
    with:
      trigger: main
    secrets: inherit
