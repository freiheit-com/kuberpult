name: "Execution Plan"
'on':
  # TODO: change to main push after test
  pull_request:
    branches:
      - main
jobs:
  execution-plan-pr:
    uses: ./.github/workflows/execution-plan-snippet.yml
    with:
      trigger: main
    secrets: inherit
