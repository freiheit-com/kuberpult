name: "Execution Plan"
'on':
  pull_request:
    branches:
      - main
jobs:
  executionplan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: google-github-actions/auth@v0 
        name: 'Authenticate to Google Cloud'
        with:
          credentials_json: ${{ secrets.FDC_CORE_CI_IMAGE_READER }} 
      - name: gcloud authorize
        run: |
          gcloud auth configure-docker
      - run: mkdir -p artifacts/
      - name: Plan execution
        run: |
          ./infrastructure/scripts/execution-plan/plan.sh ${{ github.event.pull_request.base.sha }} ${{ github.event.pull_request.head.sha }} > artifacts/execution-plan.json 
      - name: Execute 
        run: |
          # stub code, as of now just prints the plan
          echo "The execution plan is printed"
          cat artifacts/execution-plan.json
      - uses: actions/upload-artifact@v3
        with:
          name: execution-plan.json
          path: artifacts/execution-plan.json
  githubconvertor:
    needs: execution-plan
    runs-on: ubuntu-latest
    outputs:
      init: ${{ steps.plan.outputs.init }}
      build: ${{ steps.plan.outputs.build }}
      publish: ${{ steps.plan.outputs.publish }}
    steps:
      - uses: actions/download-artifact@master
        with:
          name: execution-plan.json 
          path: artifacts
      - uses: google-github-actions/auth@v0 
        name: 'Authenticate to Google Cloud'
        with:
          credentials_json: ${{ secrets.FDC_CORE_CI_IMAGE_READER }} 
      - name: gcloud authorize
        run: |
          gcloud auth configure-docker
      - name: Planner
        id: plan
        run: |
          init="$(cat artifacts/execution-plan.json | docker run -i eu.gcr.io/freiheit-core/services/github-convertor/github-convertor:0.0.1 init | tr -d '\n')"
          echo ${init}
          echo "::set-output name=init::${init}"
          build="$(cat artifacts/execution-plan.json | docker run -i eu.gcr.io/freiheit-core/services/github-convertor/github-convertor:0.0.1 build | tr -d '\n')"
          echo ${build}
          echo "::set-output name=build::${build}"
          publish="$(cat artifacts/execution-plan.json | docker run -i eu.gcr.io/freiheit-core/services/github-convertor/github-convertor:0.0.1 publish | tr -d '\n')"
          echo ${publish}
          echo "::set-output name=publish::${publish}"
      - name: print
        run: |
          echo ${{ steps.plan.outputs.init }}
          echo ${{ steps.plan.outputs.build }}
          echo ${{ steps.plan.outputs.publish }}
  print-values:
    needs: github-convertor
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ${{ needs.githubconvertor.outputs.init }}
          echo ${{ needs.githubconvertor.outputs.build }}
          echo ${{ needs.githubconvertor.outputs.publish }}

  init:
    needs: github-convertor
    strategy:
      matrix: ${{ fromJSON(needs.githubconvertor.outputs.init) }}
    runs-on: ubuntu-latest
    container: 
      image: ${{ matrix.image }}
    steps:
      - name: initialize
        run: ${{ matrix.command }}
  build:
    needs: init
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJSON(needs.githubconvertor.outputs.build) }}
    container: 
      image: ${{ matrix.image }}
    steps:
      - name: build
        run: ${{ matrix.command }}
  publish:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJSON(needs.githubconvertor.outputs.publish) }}
    container: 
      image: ${{ matrix.image }}
    steps:
      - name: publish
        run: ${{ matrix.command }}
