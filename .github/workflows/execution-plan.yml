name: "Execution Plan"
'on':
  pull_request:
    branches:
      - main
jobs:
  execution-plan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: google-github-actions/auth@v0 
        name: 'Authenticate to Google Cloud'
        with:
          credentials_json: ${{ secrets.FDC_CORE_CI_IMAGE_READER }} 
      - name: gcloud authorize
        run: |
          gcloud auth configure-docker
      - name: Get changed files
        id: changes
        run: |
          FILES_CHANGED=$(git diff --name-only --diff-filter=ACMRT ${{ github.event.pull_request.base.sha }} ${{ github.sha }})
          echo "$FILES_CHANGED"
          echo "$FILES_CHANGED" > .changedFiles
          echo 'CHANGED_FILES<<EOF' >> $GITHUB_ENV
          curl "$FILES_CHANGED" >> $GITHUB_ENV
          echo 'EOF' >> $GITHUB_ENV
          echo $CHANGED_FILES
      - name: Run Execution plan
        run: make plan

