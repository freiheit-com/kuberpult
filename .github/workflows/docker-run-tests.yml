name: docker-build-test
'on':
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
jobs:
  docker-build-test:
    runs-on: ubuntu-latest
    container: europe-west3-docker.pkg.dev/fdc-public-docker-registry/kuberpult/kuberpult-dependencies:0.1.1
    steps:
      - uses: actions/checkout@v2
      - name: Run tests (charts are not tested here)
        run: echo $PATH
      - run: make .install
      - run: echo $PATH
      - run: cd / && ls
      - run: go env GOPATH

