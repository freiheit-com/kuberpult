CREATE TABLE IF NOT EXISTS apps_teams_history (
    id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    apps VARCHAR NOT NULL,
    teams VARCHAR NOT NULL
);